# Nota de archivo — sin especificar

**Archivado**: 2026-08-14 · **Sufijo `-sin-especificar` a propósito.**

16 tareas ejecutadas sin requisito escrito detrás: este ciclo sólo tiene `tasks.md` — no
hay `proposal.md`, ni `spec.md`, ni `design.md`. Se archiva **tal cual**, sin sincronizar
nada a `openspec/specs/`, porque no hay delta que promover.
